{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run React (Vite) in Edge",
      "type": "msedge",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "vite: dev server"
    }
  ]
}
